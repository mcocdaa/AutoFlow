# ============================================
# 前端本地开发环境 (示例)
# 复制为 .env 后按需修改: cp .env.example .env
# 根目录 .env 是脚本 (start.sh) 使用的单一事实来源
# vite 只读取本目录 .env 中的 VITE_* 变量
# ============================================

# vite dev server 将 /api 代理到后端
VITE_API_PROXY_URL=http://localhost:3001
