# ============================================
# AutoFlow 仅后端模式（后端 + MySQL + Redis）
# 与 docker-compose.base.yml 合并使用
# ============================================

services:
  autoflow-backend:
    ports:
      - "${BACKEND_EXTERNAL_PORT:-3001}:3000"

networks:
  autoflow-network:
    name: autoflow-network
