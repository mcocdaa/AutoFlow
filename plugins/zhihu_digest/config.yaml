# @file /plugins/zhihu_digest/config.yaml
# @brief 知乎回答总结插件配置
# @create 2026-03-15 00:00:00

# 默认配置（可被 flow params 覆盖）
defaults:
  timeout_seconds: 30
  mode: auto
  dry_run: false

# 敏感配置：env: 前缀引用环境变量，由 Plugin.setting() 链统一解析
secrets:
  cookie: env:ZHIHU_COOKIE
