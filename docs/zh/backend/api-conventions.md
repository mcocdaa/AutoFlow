# 后端 API 约定（框架级）

本文档定义框架级 API 的通用约定，避免各模块/插件各写一套风格。

## 基本约定

- 路径：资源名用复数，层级表达归属关系
- 方法：GET 查询、POST 创建/触发、PATCH 局部更新、DELETE 删除
- 响应：统一包裹 `data` 与 `error`（或使用一致的错误结构）

## 错误与可诊断性

- 错误码与错误信息分离：对用户友好、对排障可定位
- 错误应包含 `runId`/`requestId` 便于追踪

## 幂等

- 对“触发执行”类接口建议支持幂等键（例如 header 或 body 字段）

