# 阶段四:文档 — 插件指南重写 / en↔zh 对齐 / openspec 变更记录 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers-subagent-driven-development (recommended) or superpowers-executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将插件开发指南重写为 Plugin 基类新 ABI,消除旧 register 协议残留;检查并合并 plugin-sdk.md 重叠;补齐 docs/en 插件指南;核对 en↔zh 目录对齐;按既有约定记录 openspec 变更;全文规范一致性检查。

**Architecture:** 文档按「指南(how-to,面向插件作者)↔ 规范(framework-level,面向框架边界)」双层次组织:`plugin-dev-guide.md` 重写为操作指南(新 ABI 完整示例),`specs/plugin-sdk.md` 仅更新协议描述为 Plugin 基类并删除与指南重叠的重复内容;en 版指南为 zh 的英文对照;其余文档目录仅做一致性核对(不重写);openspec 按 archive 既有格式(proposal.md + tasks.md + .openspec.yaml)归档一条本次重构记录。

**Tech Stack:** Markdown(遵循 docs/rules/docs.md:6 行 YAML 头、目录 ≤3 级、每目录 index.md、单文件 <500 行)、git。

---

## 0. 前置状态与约定

1. 阶段一/二/三已实施完成:插件为 Plugin 基类新 ABI(`plugins/common/{plugin.py, helpers.py}`、`PLUGIN = XxxPlugin` 约定、hooks.py 已删除、loader 识别 PLUGIN);后端新增 `runtime/utils/serialization.py`、删除 DB/Redis 死配置与 env_secrets;前端 api/useAsyncState 已重构。
2. 现状文件:
   - `docs/zh/plugin-dev-guide.md`(221 行,旧 register 协议)→ **重写**
   - `docs/zh/specs/plugin-sdk.md`(73 行,框架级规范,含旧协议描述)→ **更新**
   - `docs/en/`(backend/frontend/architecture/modules 各 index.md + overview.md,**无** plugin-dev-guide)→ **新增 en 指南**
   - `docs/rules/docs.md`(文档规范)、`docs/architecture/index.md`、`docs/zh/index.md`、`docs/en/index.md`
   - `openspec/changes/archive/<date>-<topic>/{proposal.md, tasks.md, .openspec.yaml}`(归档格式)
3. 约定:commit 前缀 `docs:`;每个任务独立 commit;指南示例代码必须与阶段一实际迁移结果一致(可从 `plugins/dummy/backend.py` 等文件直接核对);文档中文内容用中文(en 目录用英文)。

## 1. 文件结构

### 重写
- `docs/zh/plugin-dev-guide.md`

### 更新
- `docs/zh/specs/plugin-sdk.md`(协议描述 + 与指南分工去重)
- `docs/en/index.md`(文件列表加 plugin-dev-guide)

### 新增
- `docs/en/plugin-dev-guide.md`(zh 指南英文对照)
- `openspec/changes/archive/2026-08-10-codebase-optimization/{proposal.md, tasks.md, .openspec.yaml}`

### 核对(不重写,仅验证)
- `docs/zh/backend|frontend|architecture|modules/index.md`、`docs/en/backend|frontend|architecture|modules/index.md`(文件列表与结构一致)
- `docs/zh/modules/*.md`、`docs/en/modules/*.md`(grep 确认无旧协议残留)

---

## Task 1: 重写 docs/zh/plugin-dev-guide.md

**Files:**
- Rewrite: `docs/zh/plugin-dev-guide.md`

- [ ] **Step 1: 通读现有指南与最终代码**

Read: `docs/zh/plugin-dev-guide.md`(旧协议全貌)、`plugins/common/plugin.py`、`plugins/common/helpers.py`、`plugins/dummy/backend.py`、`plugins/zhihu_digest/backend.py`(迁移后形态)。
Note: 重写必须与这些实现完全一致。

- [ ] **Step 2: 按以下结构全文重写**(保留原 6 行 YAML 头,title 不变,version 升为 "2.0",内容全部替换)

目标结构(全文控制在 500 行内):

```
# AutoFlow 插件开发指南

## 目录
## 1. 快速入门
   - 最小插件结构(新:__init__.py + backend.py + config.yaml 可选;hooks.py 已废弃)
   - 完整最小示例(基于 plugins/dummy/backend.py 的实际代码,dummy 无 config.yaml)
## 2. Plugin 基类
   - name/version/actions/checks 类属性声明(handler 为模块级函数或 @staticmethod,类体内不能引用实例方法)
   - __init__(config) 与 config 注入(loader 解析 config.yaml 的 defaults/secrets 后传入)
   - register(registry) 由基类实现,插件无需编写
   - PLUGIN = XxxPlugin 导出约定(loader 识别;文件插件直接在单文件中定义)
## 3. Action 开发规范
   - ActionHandler 签名(ActionContext, params)
   - 类型名规则:插件名.功能
   - 完整 action 示例(带 params 校验与错误抛出)
## 4. Check 开发规范
   - CheckHandler 签名(CheckContext, params)返回 bool
   - 完整 check 示例
## 5. 共享工具(plugins/common/helpers.py)
   - 每个函数一行签名 + 用途说明:is_truthy / dry_run_enabled(ctx, params, env_var) / read_text(ctx, path, extra_roots=()) / write_text(ctx, rel_path, text) / utc_now_iso() / safe_name(name, fallback)
   - dry_run 三段式判定示例(参数 → ctx.vars → 环境变量)
   - 安全路径读写示例(白名单校验)
## 6. 配置与 Secrets(config.yaml)
   - defaults + secrets 结构(参考 plugins/zhihu_digest/config.yaml)
   - secrets 经环境变量解析注入的机制
## 7. 注册表与插件清单
   - plugins/plugins.yaml 注册(启用/禁用)
   - 加载失败不影响其他插件,错误见 GET /api/v1/plugins 的 errors 字段
## 8. 现有插件示例分析(指向各插件目录,说明各自特点)
```

示例代码要求(全部为完整可运行代码,不得出现省略号):
- 最小插件示例 = `plugins/dummy/backend.py` 的实际最终代码(照抄)
- dry_run 示例 = `dry_run_enabled` 的实际调用方式(参考 zhihu_digest 迁移后代码)
- 读写示例 = `read_text`/`write_text` 调用(参考 ai_deepseek 迁移后代码)

- [ ] **Step 3: 验证**

Run:
```bash
cd /home/mcocdaa/AI_CODE/AutoFlow
grep -n "register(registry" docs/zh/plugin-dev-guide.md
```
Expected: 无命中(全文不再描述旧协议;如有必要提到迁移历史,仅用「hooks.py 已废弃」措辞,不出现 `register(registry` 字样)。

Run: `wc -l docs/zh/plugin-dev-guide.md` — Expected: `< 500`。

- [ ] **Step 4: 示例代码一致性验证**

Run:
```bash
cd /home/mcocdaa/AI_CODE/AutoFlow/backend && .venv/bin/python - <<'EOF'
import sys; sys.path.insert(0, '..')
from plugins.dummy.backend import PLUGIN
assert PLUGIN.name == 'dummy'
print('dummy PLUGIN ok:', PLUGIN.name, PLUGIN.version)
EOF
```
Expected: `dummy PLUGIN ok: dummy 0.1.0`(证明指南示例所描述的 ABI 与实现一致)。

- [ ] **Step 5: Commit**

```bash
git add docs/zh/plugin-dev-guide.md
git commit -m "docs: rewrite plugin dev guide for Plugin base class ABI"
```

## Task 2: 更新 docs/zh/specs/plugin-sdk.md

**Files:**
- Modify: `docs/zh/specs/plugin-sdk.md`

- [ ] **Step 1: 更新协议描述**

保留框架级规范定位,做以下修改:
1. 「最小接口」段:`register(registry)` 抽象替换为 **`Plugin` 基类**(`plugins/common/plugin.py`)—— 插件声明 `name/version/actions/checks` 类属性,基类 `register(registry)` 完成注册;`PLUGIN` 类引用导出约定。
2. 「生命周期」段:`load/init/register/dispose` 更新为实际机制:`loader 识别 PLUGIN → 实例化并注入 config(init)→ plugin.register(registry)(register)→ 进程内单例(lru_cache)`;dispose 标注「当前框架未实现」。
3. 「Schema 与校验」段:标注为框架演进方向(未实现),与指南第 3 节的手工 params 校验区分。
4. 删除与指南重复的 Manifest YAML 示例(指南第 2 节已覆盖声明式类属性),规范文档只保留抽象描述。
5. 文件头 version 升为 "2.0"。

- [ ] **Step 2: 验证**

Run:
```bash
cd /home/mcocdaa/AI_CODE/AutoFlow
grep -n "register(registry\|hooks.py" docs/zh/specs/plugin-sdk.md
```
Expected: 无命中。

Run: `wc -l docs/zh/specs/plugin-sdk.md` — Expected: `< 500`。

- [ ] **Step 3: Commit**

```bash
git add docs/zh/specs/plugin-sdk.md
git commit -m "docs: update plugin-sdk spec to Plugin base class protocol"
```

## Task 3: 新增 docs/en/plugin-dev-guide.md

**Files:**
- Create: `docs/en/plugin-dev-guide.md`
- Modify: `docs/en/index.md`

- [ ] **Step 1: 创建 en 指南**

将 Task 1 完成的 zh 指南**全文翻译为英文**写入 `docs/en/plugin-dev-guide.md`(结构与示例代码一致,示例代码保留 Python 原文,说明文字英文)。头部:

```markdown
---
title: Plugin Development Guide
description: Plugin development guide for the Plugin base class ABI
keywords: [plugin, guide, sdk, development]
version: "2.0"
---
```

- [ ] **Step 2: en/index.md 文件列表补充**

在 `docs/en/index.md` 的 `## 文件列表` 下(当前为空)添加:

```markdown
- [plugin-dev-guide](plugin-dev-guide.md): Plugin development guide
```

- [ ] **Step 3: 验证**

Run:
```bash
cd /home/mcocdaa/AI_CODE/AutoFlow
grep -n "register(registry" docs/en/plugin-dev-guide.md
```
Expected: 无命中。
Run: `wc -l docs/en/plugin-dev-guide.md` — Expected: `< 500`。

- [ ] **Step 4: Commit**

```bash
git add docs/en/plugin-dev-guide.md docs/en/index.md
git commit -m "docs: add English plugin dev guide"
```

## Task 4: en↔zh 目录对齐核对

**Files:** 无修改(核对);若有发现的不一致,修正相关 index.md

- [ ] **Step 1: 结构对照**

Run:
```bash
cd /home/mcocdaa/AI_CODE/AutoFlow
diff <(ls docs/zh/backend docs/zh/frontend docs/zh/modules docs/zh/architecture docs/zh/specs) <(ls docs/en/backend docs/en/frontend docs/en/modules docs/en/architecture docs/en/specs)
```
Expected: 无输出(结构一致)。若 zh 有而 en 缺的**目录级**文件,视为已核对范围外(阶段四只保证插件指南,不新增其他翻译);若存在文件级不一致,记录并修正 index.md 文件列表。

- [ ] **Step 2: 残留检查(全 docs 排除 superpowers/plans)**

Run:
```bash
cd /home/mcocdaa/AI_CODE/AutoFlow
grep -rn "register(registry\|hooks.py" docs/zh docs/en docs/architecture --include="*.md" | grep -v superpowers || echo "clean"
```
Expected: `clean`(指南/规范已更新;架构与模块文档本就不涉及插件加载细节)。

- [ ] **Step 3: 头部格式抽查**

Run:
```bash
cd /home/mcocdaa/AI_CODE/AutoFlow
for f in docs/zh/index.md docs/zh/plugin-dev-guide.md docs/en/index.md docs/en/plugin-dev-guide.md docs/zh/specs/plugin-sdk.md docs/architecture/index.md; do head -6 "$f" | grep -q "^---" && head -6 "$f" | grep -q "title:" && echo "OK $f" || echo "BAD $f"; done
```
Expected: 全部 `OK`。

- [ ] **Step 4: Commit(如有修正)**

```bash
git add docs/zh docs/en docs/architecture
git commit -m "docs: align en/zh doc index structure"
```
(若无改动,跳过本步。)

## Task 5: openspec 变更记录归档

**Files:**
- Create: `openspec/changes/archive/2026-08-10-codebase-optimization/{proposal.md, tasks.md, .openspec.yaml}`

- [ ] **Step 1: 创建归档目录与文件**

`openspec/changes/archive/2026-08-10-codebase-optimization/proposal.md`:

```markdown
# 代码库优化重构(2026-08-10)

## 背景
插件层 hooks 样板重复、工具函数复制粘贴;后端序列化/深拷贝三处重叠、死配置残留;前端 store loading/error 样板重复;文档停留在旧 register 协议。

## 变更摘要
1. **插件**:新增 `plugins/common/`(Plugin 基类 + 6 个共享 helpers);5 个目录插件与 2 个文件示例插件迁移为 `class XxxPlugin(Plugin)` + `PLUGIN` 导出;hooks.py 全部删除;register(registry) 协议废弃。
2. **后端**:`plugin_loader` 收敛为 PLUGIN 协议;新增 `app/runtime/utils/serialization.py`(safe_deep_copy/to_jsonable),收敛 store/runner 三处重复;`PluginItem.from_info()` 工厂;删除 setting_manager DB/Redis 死配置与 env_secrets 死代码。
3. **前端**:api 层响应拦截器统一错误提取 + 类型化接口(api/plugins.ts、api/runs.ts);新增 useAsyncState composable;stores 重构为 setup store 风格。
4. **文档**:插件开发指南重写为新 ABI;plugin-sdk 规范更新;新增 en 版指南;目录对齐。

## 约束遵循
- HTTP API 兼容;零新依赖;每阶段独立验证(全量 pytest 95 passed、vue-tsc 构建通过)。

## 详细设计
见 `docs/superpowers/specs/2026-08-10-codebase-optimization-design.md` 与 `docs/superpowers/plans/2026-08-10-phase{1,2,3,4}-*.md`。
```

`openspec/changes/archive/2026-08-10-codebase-optimization/tasks.md`:

```markdown
# 任务清单

- [x] 阶段一:插件 Plugin 基类 ABI 迁移(5 目录插件 + 2 文件插件,helpers 去重,loader 识别 PLUGIN)
- [x] 阶段二:后端 serialization 统一 / from_info 工厂 / loader 收敛 / 死代码清理(95 passed)
- [x] 阶段三:前端 api 拦截器 + 类型化接口 + useAsyncState + setup store(vue-tsc 构建通过)
- [x] 阶段四:文档指南重写 / en 补齐 / openspec 归档
```

`openspec/changes/archive/2026-08-10-codebase-optimization/.openspec.yaml`(按 archive 内既有同名文件格式,例如 status 字段):

```yaml
status: done
```

(若既有 `.openspec.yaml` 字段不同,以 archive 内实际格式为准,仅保留相同字段。)

- [ ] **Step 2: 验证**

Run:
```bash
cd /home/mcocdaa/AI_CODE/AutoFlow
ls openspec/changes/archive/2026-08-10-codebase-optimization/
```
Expected: `proposal.md tasks.md .openspec.yaml` 三个文件存在。

- [ ] **Step 3: Commit**

```bash
git add openspec/changes/archive/2026-08-10-codebase-optimization/
git commit -m "docs: archive openspec change record for codebase optimization"
```

## Task 6: 阶段验收

**Files:** 无代码改动

- [ ] **Step 1: 全文残留检查**

Run:
```bash
cd /home/mcocdaa/AI_CODE/AutoFlow
grep -rn "register(registry" plugins/ docs/zh/plugin-dev-guide.md docs/en/plugin-dev-guide.md docs/zh/specs/plugin-sdk.md || echo "clean"
```
Expected: `clean`(plugins/ 无旧协议;文档无旧协议描述)。

- [ ] **Step 2: 文档规范检查**

Run:
```bash
cd /home/mcocdaa/AI_CODE/AutoFlow
for d in docs docs/zh docs/zh/specs docs/zh/modules docs/zh/backend docs/zh/frontend docs/zh/architecture docs/en docs/en/specs docs/en/modules docs/en/backend docs/en/frontend docs/en/architecture docs/architecture; do test -f "$d/index.md" && echo "OK $d" || echo "MISSING $d/index.md"; done
```
Expected: 全部 `OK`。

- [ ] **Step 3: 工作区状态**

Run: `cd /home/mcocdaa/AI_CODE/AutoFlow && git status --short && git log --oneline -10`
Expected: 干净(或仅未提交的文档改动);最近提交为本次文档阶段 commits。
