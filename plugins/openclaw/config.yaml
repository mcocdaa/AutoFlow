# @file /plugins/openclaw/config.yaml
# @brief OpenClaw 插件运行时配置

defaults:
  http_timeout: 30      # HTTP 请求超时秒数
  exec_timeout: 60      # Shell 命令执行超时秒数

secrets:
  knowflow_base_url: KNOWFLOW_BASE_URL  # KnowFlow 服务地址，对应环境变量 KNOWFLOW_BASE_URL